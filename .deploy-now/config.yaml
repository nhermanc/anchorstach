# IONOS Deploy Now — static site (workflow v1 / deploy-to-ionos)
# https://docs.ionos.space/docs/deployment-configuration/

version: 1.0

deploy:
  bootstrap:
    excludes: []

  recurring:
    excludes: []
